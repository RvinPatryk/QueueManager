<Window x:Class="QueueManager.Views.UserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:QueueManager.Converters"
        Title="Queue Manager - Panel użytkownika"
        Height="650"
        Width="1100"
        MinHeight="520"
        MinWidth="800"
        Background="#F5F5F5"
        FontFamily="Segoe UI"
        FontSize="13"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <converters:EnumDescriptionConverter x:Key="EnumDescriptionConverter"/>

        <SolidColorBrush x:Key="AppBackgroundBrush" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="CardBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderBrushSoft" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="TextMainBrush" Color="#1F2937"/>
        <SolidColorBrush x:Key="TextMutedBrush" Color="#6B7280"/>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2563EB"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#16A34A"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#F9FAFB"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextMainBrush}"/>
        </Style>

        <Style x:Key="MutedTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="Opacity"
                                        Value="0.88"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="Opacity"
                                        Value="0.75"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder"
                                        Property="Opacity"
                                        Value="0.45"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="SuccessButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="SecondaryButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextMainBrush}"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#EEEEEE"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="RowHeight" Value="42"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFAFA"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="CanUserResizeColumns" Value="True"/>
            <Setter Property="CanUserSortColumns" Value="True"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="76"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Górny pasek -->
        <Border Grid.Row="0"
                Background="White"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="0,0,0,1">

            <Grid Margin="28,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Moje zadania"
                               FontSize="24"
                               FontWeight="SemiBold"/>

                    <TextBlock Style="{StaticResource MutedTextStyle}">
                        <Run Text="Zalogowano jako: "/>
                        <Run Text="{Binding LoggedUsername, Mode=OneWay}"
                             FontWeight="SemiBold"/>
                    </TextBlock>
                </StackPanel>

                <StackPanel Grid.Column="1"
                Orientation="Horizontal"
                VerticalAlignment="Center">

                    <Border Background="#EFF6FF"
                    BorderBrush="#DBEAFE"
                    BorderThickness="1"
                    CornerRadius="14"
                    Padding="12,6"
                    Margin="0,0,12,0">

                        <TextBlock Text="Panel użytkownika"
                           Foreground="#1D4ED8"
                           FontWeight="SemiBold"
                           FontSize="12"/>
                    </Border>

                    <Button Content="Wyloguj"
                            Click="LogoutButton_Click"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Margin="0" Width="62"/>
                </StackPanel>
            </Grid>
        </Border>


        <!-- Zawartość -->
        <Grid Grid.Row="1"
              Margin="24">

            <Border Background="{StaticResource CardBrush}"
                    BorderBrush="{StaticResource BorderBrushSoft}"
                    BorderThickness="1"
                    CornerRadius="14"
                    Padding="20">


                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Nagłówek listy -->
                    <Grid Grid.Row="0"
                          Margin="0,0,0,16">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Text="Przypisane zadania"
                                       FontSize="20"
                                       FontWeight="SemiBold"/>

                            <TextBlock Text="Wybierz zadanie z listy, aby zmienić jego status."
                                       Style="{StaticResource MutedTextStyle}"
                                       Margin="0,4,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">

                            <CheckBox Content="Ukryj zakończone"
                            IsChecked="{Binding HideCompletedTasks}"
                            VerticalAlignment="Center"
                            Margin="0,0,16,0"/>

                            <Button Content="Odśwież"
                            Command="{Binding RefreshTasksCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Margin="0" Cursor="Hand"/>
                        </StackPanel>
                    </Grid>

                    <UniformGrid Grid.Row="1"
             Columns="2"
             Margin="0,0,0,16">

                        <Border Background="#EFF6FF"
            BorderBrush="#DBEAFE"
            BorderThickness="1"
            CornerRadius="10"
            Padding="14"
            Margin="0,0,8,0">

                            <StackPanel>
                                <TextBlock Text="Aktualne zadanie"
                       Foreground="#1D4ED8"
                       FontWeight="SemiBold"
                       Margin="0,0,0,6"/>

                                <TextBlock Text="{Binding CurrentTask.Nazwa, TargetNullValue='Brak zadania w trakcie'}"
                       FontSize="18"
                       FontWeight="SemiBold"/>

                                <TextBlock Text="{Binding CurrentTask.Opis, TargetNullValue=''}"
                       Foreground="{StaticResource TextMutedBrush}"
                       TextWrapping="Wrap"
                       Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Background="#F0FDF4"
            BorderBrush="#BBF7D0"
            BorderThickness="1"
            CornerRadius="10"
            Padding="14">

                            <StackPanel>
                                <TextBlock Text="Następne zadanie"
                       Foreground="#15803D"
                       FontWeight="SemiBold"
                       Margin="0,0,0,6"/>

                                <TextBlock Text="{Binding NextTask.Nazwa, TargetNullValue='Brak nowych zadań'}"
                       FontSize="18"
                       FontWeight="SemiBold"/>

                                <TextBlock Text="{Binding NextTask.Opis, TargetNullValue=''}"
                       Foreground="{StaticResource TextMutedBrush}"
                       TextWrapping="Wrap"
                       Margin="0,4,0,0"/>

                                <TextBlock Margin="0,8,0,0"
                       Foreground="{StaticResource TextMutedBrush}">
                <Run Text="Algorytm: "/>
                <Run Text="{Binding SelectedAlgorithmName, Mode=OneWay}"
                FontWeight="SemiBold"/>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </UniformGrid>

                    <!-- Akcje -->
                    <Border Grid.Row="2"
                            Background="#FAFAFA"
                            BorderBrush="{StaticResource BorderBrushSoft}"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="12"
                            Margin="0,0,0,16">

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Akcje dla zaznaczonego zadania:"
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource TextMutedBrush}"
                                       Margin="0,0,14,0"/>

                            <Button Content="Rozpocznij"
                                    Command="{Binding StartTaskCommand}"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Margin="0,0,8,0" Width="70"/>

                            <Button Content="Zakończ"
                                    Command="{Binding FinishTaskCommand}"
                                    Style="{StaticResource SuccessButtonStyle}" Width="61"/>
                        </StackPanel>
                    </Border>

                    <!-- Tabela -->
                    <DataGrid Grid.Row="3"
                              ItemsSource="{Binding Tasks}"
                              SelectedItem="{Binding SelectedTask}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              SelectionUnit="FullRow">

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID"
                                                Binding="{Binding Id}"
                                                Width="60"/>

                            <DataGridTextColumn Header="Nazwa"
                                                Binding="{Binding Nazwa}"
                                                Width="180"/>

                            <DataGridTextColumn Header="Opis"
                                                Binding="{Binding Opis}"
                                                Width="280"/>

                            <DataGridTextColumn Header="Priorytet"
                                                Binding="{Binding Priorytet}"
                                                Width="90"/>

                            <DataGridTextColumn Header="Status"
                                                Binding="{Binding Status, Converter={StaticResource EnumDescriptionConverter}}"
                                                Width="125"/>

                            <DataGridTextColumn Header="Termin"
                                                Binding="{Binding Termin, StringFormat={}{0:dd.MM.yyyy}}"
                                                Width="120"/>

                            <DataGridTextColumn Header="Czas plan."
                                                Binding="{Binding PrzewidzianyCzas}"
                                                Width="120"/>

                            <DataGridTextColumn Header="Rozpoczęcie"
                                                Binding="{Binding DataRozpoczecia, StringFormat={}{0:dd.MM.yyyy HH:mm}}"
                                                Width="155"/>

                            <DataGridTextColumn Header="Zakończenie"
                                                Binding="{Binding DataUkonczenia, StringFormat={}{0:dd.MM.yyyy HH:mm}}"
                                                Width="155"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>